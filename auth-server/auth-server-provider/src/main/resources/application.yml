# tomcat
server:
  port: 10086
  servlet:
    context-path: /

# 指定当前环境
spring:
  profiles:
    active: dev #本地开发环境
    #active: prod #本地机测试环境
  redis:
    host: 127.0.0.1
    port: 6379
    password: 123456

